import { MainContent } from "../../components/shared-styles/styles.component";
import UserProfileContent from "../../components/user-settings-components/UserProfileContent.component";

const UserSettings = () => {
  return (
    <MainContent>
      <UserProfileContent />
    </MainContent>
  );
};

export default UserSettings;
